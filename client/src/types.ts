export type User = {
  id: number;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
};

export type Article = {
  id: number;
  text: string;
  title: string;
  description: string;
};
